<div class="card" style="width: 18rem;">
  <div class="card-body">
    <h5 class="card-title"><%= @cocktail.name %></h5>
    <h6 class="card-subtitle mb-2 text-muted">XXX</h6>
    <p class="card-text">XXXX</p>
  </div>
</div>

<ul class="list-group">
  <% @cocktail.doses.each do |dose| %>
    <li class="list-group-item"><%= dose.ingredient.name %> : <%= dose.description %> <%= link_to "Delete #{dose.description} #{dose.ingredient.name}",
            dose_path(dose),
            method: :delete,
            data: { confirm: "Are you sure?" } %></li>
</ul>


</br><% end %>




<%= link_to 'Add Ingredients/Doses', new_cocktail_dose_path(@cocktail) %>
</br>
</br>
</br>
<%= link_to "Edit", edit_cocktail_path %>
</br>

<%= link_to "Go back to index", cocktails_path %>

